<Window x:Class="Task2._10_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2._10_1"
        xmlns:vm="clr-namespace:Task2._10_1.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="300">
    <Window.DataContext>
        <vm:ViewModel/>
    </Window.DataContext>
    <StackPanel Margin="10">
        <TextBlock Text="Форма входа" FontSize="15" FontWeight="Bold" Margin="5"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Text="Логин:" Margin="5"/>
            <TextBox Grid.Column="1" Margin="5" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Grid.Row="1" Text="Пароль:" Margin="5"/>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            
        </Grid>
        <Button Content="Войти" Width="80" Height="25" Margin="5" Command="{Binding AutentificationCommand}"/>
        <Border CornerRadius="5" Margin="5" Height="30" >
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers >
                        <DataTrigger Binding="{Binding IsSuccess}" Value="True" >
                            <Setter Property="Background" Value="Green"/>
                        </DataTrigger>
                        
                        <DataTrigger Binding="{Binding IsSuccess}" Value="False" >
                            <Setter Property="Background" Value="#FFD22828"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock  Text="{Binding StatusMessage}" TextAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" />
        </Border>
    </StackPanel>
</Window>
