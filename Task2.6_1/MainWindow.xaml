<Window x:Class="Task2._6_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2._6_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="300">
    <Window.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <StackPanel Margin="10">
            <TextBox x:Name="MainTextBox"/>
            <StackPanel Orientation="Horizontal" Margin="5">
                <CheckBox x:Name="DetailsCheckBox"/>
                <TextBlock Text="Показать детали"/>
            </StackPanel>
            <StackPanel Visibility="{Binding ElementName=DetailsCheckBox, Path=IsChecked, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="NumberOfCharactersTextBox" Text="{Binding ElementName=MainTextBox, Path=Text.Length}" />
                <ProgressBar Value="{Binding ElementName=NumberOfCharactersTextBox, Path=Text}" Maximum="100" Height="15" />
            </StackPanel>
            
        </StackPanel>

    </Grid>
</Window>
