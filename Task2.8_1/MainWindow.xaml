<Window x:Class="Task2._8_1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task2._8_1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <local:NameConverter x:Key="NameConverter"/>

        <DataTemplate x:Key="ListBoxTemplate" >
            <Border x:Name="Border" Background="LightGreen" BorderThickness="1" CornerRadius="10" Padding="10" >
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image   Width="100" Height="100" Source="{Binding Name, Converter={StaticResource NameConverter}}"/>
                    <StackPanel  Grid.Column="1">
                        <TextBlock  Text="{Binding Name}"/>
                        <TextBlock  Text="{Binding Price}"/>
                    </StackPanel>
                   
                </Grid>
                

            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding Category}">
                    <DataTrigger.Value>
                        <local:ProductCategory>Appliances</local:ProductCategory>
                    </DataTrigger.Value>
                    <Setter TargetName="Border" Property="Background"  Value="LightBlue"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListBox ItemsSource="{Binding Products}" ItemTemplate="{StaticResource ListBoxTemplate}" HorizontalContentAlignment="Stretch"/>
    </Grid>
</Window>
